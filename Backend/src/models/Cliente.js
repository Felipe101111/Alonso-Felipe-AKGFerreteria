class Cliente {
    constructor(id, nombre, email, telefono, direccion, usuarioId) {
        this.id = id;
        this.nombre = nombre;
        this.email = email;
        this.telefono = telefono;
        this.direccion = direccion;
        this.usuarioId = usuarioId;
    }
}

export default Cliente;
